<%= form_for([@restaurant, @restaurant.meals.build]) do |f| %>
  <p>
    <%= f.label :price %>
    <%= f.text_field :price %>
  
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  
    <%= f.submit %>
  </p>
<% end %>