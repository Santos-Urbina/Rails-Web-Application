<%= form_for([@restaurant, @restaurant.reviews.build]) do |f| %>
  <%= f.label :reviewer %>
  <%= f.text_field :reviewer %>
  <%= f.label :body %>
  <%= f.text_area :body %>
  <%= f.submit %>
<% end %>